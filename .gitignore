# ── Swift / SwiftPM build artifacts ──────────────────────────────
.build/
.swiftpm/
Package.pins
DerivedData/
*.xcodeproj
xcuserdata/

# ── Build & distribution outputs (shipped via GitHub Releases) ────
Pith.app/
*.app/
*.zip
*.dmg

# ── macOS noise ───────────────────────────────────────────────────
.DS_Store

# ── Python tooling cache (Scripts/) ───────────────────────────────
__pycache__/
*.pyc

# ── Internal tooling, never published ─────────────────────────────
# AI/agent session state and planning artifacts
.claude/
.remember/
docs/superpowers/

# graphify skill output (local only)
graphify-out/
