# Swift / SwiftPM build artifacts
.build/
.swiftpm/
Package.pins
*.xcodeproj
DerivedData/

# Built app bundle (distributed via GitHub Releases, not committed)
WhisperDict.app/
*.app/
*.zip

# macOS
.DS_Store

# Claude Code / local session state (personal, do not publish)
.claude/
.remember/

# Distributable disk images (attached to Releases, not committed)
*.dmg
